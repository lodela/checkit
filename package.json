{
  "name": "checkit-webapp",
  "version": "1.3.0-beta",
  "description": "CheckIt - Menú digital interactivo para restaurantes con jQuery + Bootstrap",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html",
    "server": "json-server --watch db.json --port=3001",
    "start": "npm run server & npm run dev",
    "build": "echo 'Building for production...' && echo 'Build complete!'",
    "format": "prettier --write \"**/*.{js,css,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,md}\"",
    "lint:fix": "eslint assets/js/**/*.js --fix",
    "lint": "eslint assets/js/**/*.js"
  },
  "keywords": [
    "restaurant",
    "menu",
    "qr",
    "jquery",
    "bootstrap",
    "90s",
    "retro",
    "webapp"
  ],
  "author": "Developer 10X",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.3",
    "json-server": "^0.17.4",
    "live-server": "^1.2.2",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
